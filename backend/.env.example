# Environment Configuration
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO
LOG_FORMAT=console

# API Configuration
API_TITLE="Province Backend"
API_VERSION="0.1.0"
ALLOWED_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]

# AWS Configuration (optional for development)
AWS_REGION=us-east-1
AWS_PROFILE=default

# DynamoDB Tables (for future use)
MATTERS_TABLE_NAME=matters
DOCUMENTS_TABLE_NAME=documents
PERMISSIONS_TABLE_NAME=permissions
DEADLINES_TABLE_NAME=deadlines

# S3 Configuration (for future use)
DOCUMENTS_BUCKET_NAME=province-documents-storage
TEMPLATES_BUCKET_NAME=province-templates-storage

# OpenSearch Configuration (for future use)
OPENSEARCH_ENDPOINT=
OPENSEARCH_INDEX_NAME=legal-documents

# Bedrock Configuration (for AI features)
BEDROCK_REGION=us-east-1
BEDROCK_MODEL_ID=anthropic.claude-3-sonnet-20240229-v1:0

# KMS Configuration (for future use)
KMS_KEY_ALIAS=alias/province

# EventBridge Configuration (for future use)
EVENTBRIDGE_BUS_NAME=province-events

# SNS Configuration (for future use)
SNS_TOPIC_ARN=